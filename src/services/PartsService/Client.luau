-- -- CONFIGURATION --
-- local PositionKeyCapUp = 0.6
-- local PositionKeyCapDown = 0.3

-- local KeyCapsConnections = {}
-- local PartsServiceClient = {}

-- if not game:IsLoaded() then
-- 	game.Loaded:Wait()
-- end

-- function PartsServiceClient.init()
-- 	task.spawn(function()
-- 		for _, Part in pairs(workspace.SpecialParts:GetChildren()) do
-- 			if Part:IsA("Model") then
-- 				PartsServiceClient.PartTouched(Part)
-- 			end
-- 		end
-- 	end)
-- end

-- function PartsServiceClient.PartTouched(KeyCap)
-- 	local PartTouched: boolean = false

-- 	warn(KeyCap)
-- 	warn(KeyCap:FindFirstChildOfClass("Part"))
-- 	if KeyCap and KeyCap:FindFirstChildOfClass("Part") then
-- 		KeyCap:FindFirstChildOfClass("Part").Touched:Connect(function(Hit: BasePart)
-- 			if Hit.Parent:FindFirstChildOfClass("Humanoid") then
-- 				if not PartTouched then
-- 					PartsServiceClient.PlayTween(KeyCap.Parent)
-- 					PartTouched = true
-- 					task.delay(0.1, function()
-- 						PartTouched = false
-- 					end)
-- 				else
-- 					return
-- 				end
-- 			end
-- 		end)
-- 		KeyCap:FindFirstChildOfClass("Part").TouchEnded:Connect(function(Hit: BasePart)
-- 			if Hit.Parent:FindFirstChildOfClass("Humanoid") then
-- 				PartsServiceClient.PlayTween(KeyCap.Parent)
-- 			end
-- 		end)
-- 	else
-- 		if KeyCap then
-- 			local Part = KeyCap:FindFirstChildOfClass("Part")
-- 			PartsServiceClient.PartTouched(Part)
-- 		end
-- 	end
-- end

-- function PartsServiceClient.PlayTween(KeyCap: Model)
-- 	local Position = KeyCap:GetPivot()
-- 	if Position.Y == PositionKeyCapUp then
-- 		local Connection = task.spawn(function()
-- 			for i = 0.1, 1, 0.1 do
-- 				KeyCap:PivotTo(CFrame.new(Position.X, math.lerp(PositionKeyCapUp, PositionKeyCapDown, i), Position.Z))
-- 				task.wait()
-- 			end
-- 		end)
-- 		KeyCapsConnections[KeyCap] = Connection
-- 	end
-- 	if Position.Y == PositionKeyCapDown then
-- 		local Connection = task.spawn(function()
-- 			for i = 0.1, 1, 0.1 do
-- 				KeyCap:PivotTo(CFrame.new(Position.X, math.lerp(PositionKeyCapUp, PositionKeyCapDown, i), Position.Z))
-- 				task.wait()
-- 			end
-- 		end)
-- 		KeyCapsConnections[KeyCap] = Connection
-- 	end
-- 	if Position.Y ~= PositionKeyCapUp and Position.Y ~= PositionKeyCapDown then
-- 		if KeyCapsConnections[KeyCap] then
-- 			task.cancel(KeyCapsConnections[KeyCap])
-- 		end
-- 		local Connection = task.spawn(function()
-- 			for i = 0.1, 1, 0.1 do
-- 				KeyCap:PivotTo(CFrame.new(Position.X, math.lerp(PositionKeyCapDown, PositionKeyCapUp, i), Position.Z))
-- 				task.wait()
-- 			end
-- 		end)
-- 		KeyCapsConnections[KeyCap] = Connection
-- 	end
-- end

-- return PartsServiceClient
